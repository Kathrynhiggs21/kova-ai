# Run Zoo V8

Open **Actions → Zoo V8 Complete Image Pipeline → Run workflow**.

## Free image-producing proof

- generation mode: `mock`
- card IDs: `A001,A002,A011,A014`
- concurrency: `4`
- overwrite: `true`

The artifact contains master PNGs, app WebPs, thumbnails, a contact sheet, source/QA reports, and `cards-v8.json`.

## Production-art proof

Use `gemini` mode with one fully ready ID such as `A014`. Paid generation is blocked for incomplete records. Gemini creates the art plate only; the compositor applies exact final text and exports all image sizes.
